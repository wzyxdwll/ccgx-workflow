---
phase: v4.1-roadmap
plan: .claude/plan/v4.1-roadmap.md
goal: 综合 v4.0 dogfood 反馈 + 用户 6 问 + 开源生态扫描，定 v4.1 phase 列表
created_at: 2026-05-04T00:00:00Z
sources:
  - .ccg-research/01-context-architecture.md (GSD context 治理)
  - .ccg-research/04-ecosystem-scan.md (开源生态反模式)
  - .ccg-research/07-multimodel-collaboration-rethink.md (多模型生硬 motivation)
  - .ccg/roadmap.md (v4.0 dogfood Milestone Summary)
multi_model_skipped_reason: |
  本规划是 meta 设计讨论（GSD 行为澄清 + v4.1 缺陷盘点），不是代码实施。
  双模型独立分析对 GSD 行为的了解不会比 .ccg-research/ 更多，并行 spawn
  会浪费 token。主线综合既有研究文档 + 用户痛点直接出 plan 更高 ROI。
---

# 📋 v4.1 实施规划

## 任务定位

**v4.0 已完整交付**（12 phase 全过 + dogfood 验证），v4.1 解决 v4.0 暴露的体验问题与设计缺陷。本 plan 综合：

- 用户在 `/ccg:plan` 中提的 6 个问题（GSD 行为澄清 + 体验痛点）
- v4.0 dogfood 实测沉淀（Phase 1 沙箱 / Phase 2 subagent 嵌套限制 / Phase 12 +2% 主线增量异常）
- 开源生态扫描的反模式清单
- 多模型协作 motivation 文档已立的 4 个改进方向

---

## v4.1 缺陷盘点（来源对应到具体文档）

### A. 体验类（用户直接感知）

| # | 缺陷 | 来源 | 优先级 |
|---|------|------|--------|
| A1 | autonomous 顺序串行，12 phase ~2 hr | dogfood 实测 + 用户 Q2 | ⭐⭐⭐ |
| A2 | 多模型协作生硬（4 子项见 .ccg-research/07）| 用户 Q3 + 07 文档 | ⭐⭐⭐ |
| A3 | impeccable 20 命令 description 英文，命令面板中英混排 | 用户上一轮反馈 | ⭐⭐ |
| A4 | `/clear` 后不自动恢复项目状态（缺 SessionStart hook 注入）| 用户 Q6 + GSD 对照 | ⭐⭐⭐ |
| A5 | 5 个 v3 命令残留 ~/.claude/commands/ccg/ 不会被 ccg init 删除 | dogfood 实测 | ⭐ |

### B. 架构类（dogfood 沉淀）

| # | 缺陷 | 来源 | 优先级 |
|---|------|------|--------|
| B1 | `general-purpose` subagent 不能嵌套 spawn `Agent`，phase-runner G 方案"双层"实际是 fallback | dogfood Phase 2 实测 | ⭐⭐ |
| B2 | phase-runner subagent 注册需重启 Claude Code，不能即时 | dogfood Step 4 实测 | ⭐ |
| B3 | context-monitor hook 只警告不改变行为（GSD 会提示自然断点保存）| 用户 Q5 + 01 文档 | ⭐⭐ |
| B4 | autonomous 没有 wave 并行调度（依赖图静态可分析）| 用户 Q2 + 04 ROI #14 | ⭐⭐⭐ |

### C. Skill 体系类（开源生态反模式）

| # | 缺陷 | 来源 | 优先级 |
|---|------|------|--------|
| C1 | 100+ skill description 总长撞 1% 上下文预算未审计 | 04 #2 + #3 | ⭐⭐ |
| C2 | 没用 `context: fork` 隔离重型 skill | 04 反模式 #9 + 7.4 节 | ⭐⭐ |
| C3 | 没用 `paths` 限定 skill 激活范围（如 frontend-design） | 04 反模式 #19 | ⭐ |
| C4 | 命令面板 31 仍超 Anthropic 推荐 ≤22 | 04 #1 + 9.1 节 | ⭐ |

---

## v4.1 Phase 列表（按价值 / 工时排序）

### v4.1-α（高 ROI 集中处理）— 5 days

#### Phase 1：SessionStart hook + 项目记忆自动注入（A4，用户 Q6）

- **Goal**：新会话启动时 Claude Code **自动 Read** `.ccg/roadmap.md` / `.ccg/state.md` / 当前 active phase 的 `.context/<phase>/SUMMARY.md`，注入精简摘要到主线 context
- **机制**：写 `templates/hooks/ccg-session-state.js`（参考 GSD `gsd-session-state.sh`），settings.json hooks 段加 `SessionStart` 注册
- **Acceptance**：
  - 新会话启动时 statusline 显示当前 phase / next action（已部分实现）
  - PostStart 注入 `additionalContext` 含 ROADMAP 头部 20 行 + 当前 phase 状态
  - 单测：mock SessionStart 输入，验证 hook 输出格式
- **工时**：1 天
- **Type**：backend

#### Phase 2：autonomous wave 并行调度（A1, B4，用户 Q2）

- **Goal**：autonomous 命令加 `--parallel [--max-concurrent N]` flag，按依赖图拓扑分波，波内并行 spawn phase-runner
- **机制**：
  - 解析 `.ccg/roadmap.md` 每 phase 的 `Depends on` 字段，建依赖图
  - Kahn 算法分波（无依赖 → wave 1，依赖 wave 1 → wave 2...）
  - 波内并行 spawn 多个 phase-runner（Claude Code 原生支持）
  - 主线管多个 task notification（completion 时进 4.4 推进）
  - 跨 wave 依赖断裂自动 cascade skip
- **Acceptance**：
  - `templates/commands/autonomous.md` 加 Step 4.0 拓扑排序 + `--parallel` 路径
  - 单测：构造 12 phase 依赖图，验证波划分正确（roadmap.md 当前依赖图：Wave 1 = 1, 3, 4, 7, 8, 10, 11；Wave 2 = 2, 5, 6 等）
  - 实测：用 v4.1 自身 dogfood 跑（如果有未完成 phase），验证墙钟时间压缩 30-40%
- **工时**：2 天
- **Type**：backend

#### Phase 3：specialist matrix 路由（A2 改进 A，多模型生硬主修复）

- **Goal**：从"按文件类型分"（前/后端）升级到"按角色 × 层"二维路由
- **机制**：6 个核心命令（plan / analyze / debug / review / optimize / test）加 `--role=architect|critic|implementer|tester|writer` flag
- **路由矩阵**：

  ```
  | Role × Layer  | architect      | critic              | implementer | tester        | writer |
  |---------------|----------------|---------------------|-------------|---------------|--------|
  | backend       | codex (xhigh)  | codex (adversarial) | codex       | codex (tester)| claude |
  | frontend      | gemini         | gemini (adversarial)| gemini      | gemini        | gemini |
  | fullstack     | codex+gemini   | both debate         | runner 决   | runner 决     | claude |
  ```

- **Acceptance**：
  - 6 个核心命令模板含 role flag 解析
  - `templates/prompts/{codex,gemini}/` 已有 6 角色 prompt 库（v3.0 就位）—— 直接复用
  - 单测：mock 命令 input + role，验证选对 prompt 文件
- **工时**：2 天
- **Type**：backend

### v4.1-β（中 ROI 集成处理）— 4 days

#### Phase 4：dedicated challenger 主线扁平编排（A2 改进 C，引擎约束适配）

- **背景**：原 v4.0 Phase 1.5 设计想在 phase-runner 内 spawn `assumptions-analyzer` 做"内置 critic"。**但 2026-05-04 nested-spawn 测试证伪**——Claude Code 引擎层禁止 subagent spawn `Agent`（commit `a7cdffd`）。改为**主线扁平化编排**。
- **Goal**：critical phase 在 phase-runner（implementer）完成后，由**主线**追加 spawn `assumptions-analyzer` / `nyquist-auditor`，让 challenger 用独立 fresh context 挑战 implementer 的产出，找到 critical 让 implementer 修订一次
- **机制（扁平化 + plugin 双视角 advisor）**：
  ```
  主线 spawn phase-runner (implementer)  ← fresh ctx
       ↓ 摘要返回
  if phase.frontmatter.Critical == true:
      主线并行 spawn:
        ├─ Agent(subagent_type="codex:codex-rescue", prompt=challenge phase 改动)  ← codex 后端视角 advisor
        ├─ Agent(subagent_type="gemini:gemini-rescue", prompt=challenge phase 改动) ← gemini 前端/UX 视角 advisor
        └─ Agent(subagent_type="assumptions-analyzer" / "nyquist-auditor")           ← CCG specialist critic
      ↓ 三方反馈 + 主线综合（每个 ≤200 token 摘要）
      if critical findings:
          主线 spawn phase-runner (implementer, 含 critical 反馈) → 修订
      else:
          推进
  else:
      直接推进（无 challenger）
  ```
- **关键决策**：用 **`codex:codex-rescue` / `gemini:gemini-rescue` plugin** 做双视角 advisor，**不**用 codeagent-wrapper（理由见 v4.0.1 客观评价：plugin 7 项胜出 / codeagent 唯一胜出"沙箱 bypass"在 advisor 场景用不上，因为 advisor 不需要 git/test）
- **Acceptance**：
  - 改 `templates/commands/autonomous.md` Step 4.4 推进逻辑加 challenger 分支判定
  - roadmap.md schema 加 `Critical: true|false` 字段（默认 false，避免每 phase 都付 4-spawn cost）
  - challenger 选择规则：
    - phase_type=backend + Critical=true → spawn codex:codex-rescue + assumptions-analyzer（双 advisor）
    - phase_type=frontend + Critical=true → spawn gemini:gemini-rescue + nyquist-auditor
    - phase_type=fullstack + Critical=true → spawn 双 plugin advisor + 双 specialist
  - 单测：mock phase 标 Critical=true，验证主线 spawn plugin advisor + specialist；mock 反馈含 critical findings，验证修订循环
  - 降级：plugin 没装时主线回退到只 spawn specialist（不调 codeagent，避免重新建立依赖）
- **工时**：1 天
- **Type**：backend

#### Phase 5：原生 debate 原语（A2 改进 B，主线编排）

- **背景**：同 Phase 4——引擎层禁止 subagent 嵌套，多轮对辩**必须主线编排**。
- **Goal**：新命令 `/ccg:debate <topic>` 由主线管 A↔B 多轮对辩（spawn → 接 200-token 摘要 → spawn → 接摘要，cap 3 轮）
- **机制（主线编排 + plugin 双 agent）**：
  ```
  主线: spawn Agent(subagent_type="codex:codex-rescue", prompt=propose) → 接 ≤200 token propose 摘要
  主线: spawn Agent(subagent_type="gemini:gemini-rescue", prompt=challenge + codex.propose) → 接 challenge 摘要
  主线: spawn Agent(subagent_type="codex:codex-rescue", prompt=respond + gemini.challenge) → 接 respond
  ...
  cap 3 轮 或 challenger 自报"无 critical" 即停
  主线综合 → 最终方案 + 分歧点列表
  ```
- **关键决策**：用 **codex/gemini plugin**（不是 general-purpose subagent，也不是 codeagent-wrapper）。理由：plugin 内部 prompt 工程对 codex / gemini 各自做了精细优化（adversarial-review 等模式现成可用），主线只接 200-token 摘要，context 不漂移
- **Acceptance**：
  - 新建 `templates/commands/debate.md`（命令模板，含主线编排状态机）
  - 主线管多轮 spawn + 摘要解析 + 收敛判定
  - 单测：mock 多轮 propose/challenge/respond，验证 cap 3 触发停止
  - 单测：mock B 自报"无 critical" 即提前停止
  - 降级：plugin 没装时降到 general-purpose subagent + CCG 自己的 prompt 模板
- **工时**：3 天
- **Type**：backend

### v4.1-γ（低 ROI 收尾）— 3 days

#### Phase 6：清理残留 + 命令面板瘦身（A5, C4）

- **Goal**：
  - ccg init 加 sync 模式 flag（`--sync` 删 ~/.claude 中已不在新模板的文件）
  - 命令面板 31 → 22（合并 / 移到 skill / 删除）
- **机制**：
  - installer.ts 加 sync 路径
  - team-research / team-plan / team-review 折回 `/ccg:team` 子命令（独立调用率低）
  - extract-learnings / forensics / health / map-codebase 移到 skill（user-invocable: true，不进 commands/）
- **Acceptance**：
  - --sync flag 工作 + 单测验证不删用户自建文件
  - 命令面板从 31 降到 22 ± 2
- **工时**：1.5 天
- **Type**：backend

#### Phase 7：Skill 体系优化（C1, C2, C3, A3）

- **Goal**：
  - **C1**：审计 100+ skill description 总长，超 1% 预算的截短到 ≤80 字符
  - **C2**：domains/ + impeccable/ 重型 skill 加 `context: fork` frontmatter
  - **C3**：`frontend-design` 等加 `paths: "*.tsx,*.vue"` 限定
  - **A3**：impeccable 20 description 翻译成中文
- **Acceptance**：
  - 跑 `du -sh ~/.claude/skills/ccg/**/SKILL.md` 总 description 长度脚本审计
  - skill-registry.ts 单测覆盖新 frontmatter 字段解析
- **工时**：1.5 天
- **Type**：backend

### v4.1-δ（codeagent 退役）— 3 days（v4.0.1 校正后新增）

#### Phase 8：codeagent-wrapper → plugin 迁移（命令面板核心路径升级）

- **背景**：v4.0.1 nested-spawn 测试 + 客观对比表明：codex/gemini plugin 在 7 项维度优于 codeagent-wrapper（异步 / 主线 context 友好 / prompt 工程 / 上游维护 / subagent 类型注册 / Hook 集成 / task_id），唯一胜出"沙箱完全 bypass"在双模型分析场景用不上（advisor 不需要 git/test）。codeagent-wrapper 是 v3.0 留下的同步 Bash 调用层，**不再是 v4.0+ 推荐路径**。
- **Goal**：把 6 个核心命令（plan / execute / analyze / optimize / test / review）从 `Bash(codeagent-wrapper --backend codex/gemini)` 迁移到 `Agent(subagent_type="codex:codex-rescue") + Agent(subagent_type="gemini:gemini-rescue")`，主线只接 ≤200 token 摘要。
- **Acceptance**：
  - 6 个命令模板改写：
    - `templates/commands/plan.md` Phase 2 双模型并行 → plugin spawn
    - `templates/commands/execute.md` 实施阶段双模型并行 → plugin spawn
    - `templates/commands/analyze.md` / `optimize.md` / `test.md` / `review.md` 同
  - **codeagent-wrapper 标 deprecated**：`templates/scripts/invoke-model.mjs` + 51 处模板调用保留作 BC，但模板里加注释 "deprecated_in: v4.1, replaced_by: Agent(codex:codex-rescue)"
  - **降级路径**：用户没装 codex/gemini plugin 时，模板自动 fallback 到 codeagent-wrapper Bash 调用（保持 BC）
  - 单测：mock plugin 装/未装两种情况，验证模板路径切换正确
  - 实测：用 v4.1 新版 `/ccg:plan` 跑同等任务，主线 context 增量从 v4.0 的 +5% 降到 +1.5%（plugin 摘要替代 stdout 全文）
- **预期收益**：
  - 主线 context 漂移降幅 ~70%（同任务规模）
  - plugin 自家 prompt 工程提升模型输出质量（adversarial-review 等专用模式可用）
  - CCG 卸下 870 行 invoke-model.mjs 的维护负担（虽然 BC 保留但不再是默认）
- **工时**：3 天
- **Type**：backend
- **Critical**：true（破坏性路径切换，需 challenger）

### v4.1 全程总工时

**15 days**（α 5 + β 4 + γ 3 + δ 3）—— 比 v4.0 (3-4 周) 快很多，因为 v4.0 基础设施（phase-runner / .context state machines）已经全部就位，v4.1 是在它们上面做精度提升 + codeagent 退役。

---

## 关键文件（变更预估）

| 文件 | 操作 | 说明 |
|------|------|------|
| `templates/hooks/ccg-session-state.js` | 新建 | Phase 1 项目记忆 |
| `templates/commands/autonomous.md` | 修改 | Phase 2 加 `--parallel` |
| `templates/commands/{plan,analyze,debug,review,optimize,test}.md` | 修改 | Phase 3 加 `--role` flag |
| `templates/commands/agents/phase-runner.md` | 修改 | Phase 4 challenger 钩子激活 |
| `templates/commands/debate.md` | 新建 | Phase 5 |
| `src/utils/installer.ts` | 修改 | Phase 6 `--sync` 模式 |
| `src/utils/skill-registry.ts` | 修改 | Phase 7 `context: fork` / `paths` 字段解析 |
| `templates/skills/impeccable/*/SKILL.md` × 20 | 修改 | Phase 7 description 翻译 |
| `src/utils/__tests__/` | 新增 | 每 phase 配套测试 |

---

## 风险与缓解

| 风险 | 缓解措施 |
|------|----------|
| Phase 2 wave 并行让主线 context 涨快 | 每个 spawn 仍走 phase-runner（fresh subagent），主线只接 ≤200 token 摘要；理论增量保持 +1%/wave 而不是 +1%/phase |
| Phase 3 路由矩阵改 6 个命令风险大 | 增量改：先做 plan / review 两个高频，验证后扩展；保留 v4.0 的 `{{BACKEND_PRIMARY}}/{{FRONTEND_PRIMARY}}` 兼容路径 |
| Phase 5 debate 收敛判定主观 | 引入"B 自报无 issue"+ 输出长度变化双信号，cap 3 强制止损 |
| Phase 7 skill description 翻译丢失原意 | 翻译完跑一次单测验证 description.length ≤80，且关键词覆盖原文 |

---

## 不进 v4.1 的项（明确 cut）

- v4.0 Phase 1.5 暴露的 `general-purpose` subagent 嵌套限制（B1）：Claude Code 引擎层限制，CCG 改不了。phase-runner.md 已经写了 fallback 路径（实测有效）。**v4.1 不解决**。
- v4.0 Phase 12 主线 +2% 异常（docs phase 主线参与多）：纯文档 phase 性质决定，无优化空间。**不进 v4.1**。
- subagent 注册需重启（B2）：同样是 Claude Code 引擎限制。**不进 v4.1**，但写进 README 让用户预期对齐。

---

## 推荐启动顺序

```
Phase 1 (1d) → Phase 4 (1d，用 Phase 1 的 hook 模式参考)
                    ↓
Phase 2 (2d) ←─── Phase 3 (2d) ─── Phase 7 (1.5d, 跟 Phase 3 并行)
                    ↓
                    Phase 5 (3d, 用 Phase 3 路由能力)
                    ↓
                    Phase 6 (1.5d 收尾)
```

依赖图上 Phase 2 / Phase 3 / Phase 7 可以并行（v4.0 自身的 wave 调度还没做，这次 v4.1 dogfood 自己用 Phase 2 完成后的能力跑后续——递归 dogfood）。

---

## SESSION_ID 占位

> 本规划跳过双模型并行（理由见 frontmatter `multi_model_skipped_reason`），无 CODEX_SESSION / GEMINI_SESSION。

> 未来 `/ccg:execute .claude/plan/v4.1-roadmap.md` 时如需多模型分析，主线会按各 phase 的 Type 字段重新 spawn。

---

## v4.0 → v4.1 哲学转变

| v4.0 | v4.1 |
|------|------|
| **基础设施落地** — phase-runner / state machine / async triplet | **使用体验精修** — wave 调度 / 项目记忆 / 多模型对辩 / skill 优化 |
| GSD 借鉴（subagent / context 治理） | Anthropic Skills 官方机制深用（context: fork / paths / progressive disclosure） |
| 单 phase 串行 | 多 phase 并行 |
| 模板硬编码 | role-based 路由 |

v4.0 是"立柱搭梁"，v4.1 是"墙面装饰"——基础结构不动，体验细节大幅升级。

**Last Updated**：2026-05-04
